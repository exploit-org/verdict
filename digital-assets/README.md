# Digital assets

`org.exploit.verdict:digital-assets` includes `evm`, `bitcoin`, `tron`, `solana`,
and `xrp`. Each is also available separately under `org.exploit.verdict`.

See the [intent guides](../docs/intents/README.md) for supported transactions and policy fields.
