# Agentic payments

`org.exploit.verdict:agentic-payments` includes `ap2` and `mc-vi`. The `payments`
module supplies their shared policy functions. All three are also available
separately under `org.exploit.verdict`.

See the [payment policy guide](../docs/intents/payments.md).
